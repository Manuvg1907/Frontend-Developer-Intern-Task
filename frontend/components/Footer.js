export default function Footer() {
    return (
        <footer className="bg-gray-700 text-white text-center py-3 fixed bottom-0 w-full">
            <p>ScalableWebApp &copy; {new Date().getFullYear()}</p>
        </footer>
    );
}
